<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />  <!-- Background color -->
            <corners android:radius="64dp" />  <!-- Adjust radius for roundness -->
        </shape>
    </item>
</selector>